-- 1) Nomes dos clientes de um estado indicado como parâmetro.
CREATE PROCEDURE 'Clientes & Estado'
    @clientes 1 VarChar(30),
    @estados 2 Char(2),
AS
BEGIN
    SET @clientes = (
        SELECT
          FROM
         WHERE 
    )