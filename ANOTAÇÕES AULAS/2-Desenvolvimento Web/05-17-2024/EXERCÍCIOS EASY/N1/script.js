function PegaDados(frmDados) {
    let nome = frmDados.txt_Nome.value;
    let idade = frmDados.txt_Idade.value;

    if (idade >= 18) {
        alert(nome + ", vai toma no cu vet, agora tu paga contakkkkkkk");
    } else {
        alert("Puta que pariu " + nome + ", você ainda é cabaço");
    }
}
